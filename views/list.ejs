<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Mi Dashboard</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">
    
    <style>
        /* FONDO ANIMADO */
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(-45deg, #ee7752, #e73c7e, #23a6d5, #23d5ab);
            background-size: 400% 400%;
            animation: gradient 15s ease infinite;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
            /* Esto centra todo si hay pocas listas */
            display: flex;
            flex-direction: column;
            align-items: center; 
        }
        @keyframes gradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        h1.main-title {
            color: white;
            text-transform: uppercase;
            letter-spacing: 5px;
            margin-bottom: 40px;
            text-shadow: 0 4px 10px rgba(0,0,0,0.2);
        }

        /* CONTENEDOR FLEXIBLE (Para poner las cajas lado a lado) */
        .dashboard {
            display: flex;
            flex-wrap: wrap; /* Si no caben, bajan a la siguiente línea */
            gap: 40px;       /* Espacio entre cajas */
            justify-content: center;
            width: 100%;
        }

        /* CAJA DE CRISTAL (Individual) */
        .box {
            background: rgba(255, 255, 255, 0.25);
            box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
            backdrop-filter: blur(8px);
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.18);
            width: 350px; /* Ancho fijo para que queden iguales */
            padding: 30px;
            display: flex;
            flex-direction: column;
        }

        h2 {
            color: white;
            text-align: center;
            text-transform: capitalize;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
            font-size: 2rem;
            margin-top: 0;
            margin-bottom: 20px;
            border-bottom: 1px solid rgba(255,255,255,0.3);
            padding-bottom: 10px;
        }

        .item {
            background: rgba(255, 255, 255, 0.8);
            border-radius: 10px;
            padding: 10px 15px;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            transition: 0.2s;
        }
        .item:hover { transform: scale(1.02); background: white; }

        input[type="checkbox"] {
            margin-right: 15px;
            accent-color: #e73c7e;
            cursor: pointer;
            width: 18px; height: 18px;
        }

        p { margin: 0; font-size: 16px; color: #333; word-break: break-all; }

        /* FORMULARIO AÑADIR */
        .form-add { margin-top: auto; display: flex; gap: 10px; padding-top: 20px;}
        
        input[type="text"] {
            flex: 1; padding: 10px; border: none; border-radius: 8px; outline: none; font-family: 'Poppins', sans-serif;
        }
        
        button {
            width: 40px; height: 40px; border-radius: 50%; border: none;
            background-color: #e73c7e; color: white; font-size: 24px;
            cursor: pointer; display: flex; justify-content: center; align-items: center; padding-bottom: 4px;
        }
        button:hover { background-color: #23a6d5; }

        /* SECCIÓN PARA CREAR NUEVA LISTA */
        .new-list-section {
            margin-top: 50px;
            text-align: center;
        }
        .btn-create {
            padding: 15px 30px;
            border-radius: 30px;
            background: white;
            color: #e73c7e;
            text-decoration: none;
            font-weight: bold;
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
            transition: 0.3s;
        }
        .btn-create:hover { background: #e73c7e; color: white; }
    </style>
</head>
<body>

    <h1 class="main-title">Mis Tareas</h1>

    <div class="dashboard">

        <% allLists.forEach(function(list){ %>
            
            <div class="box">
                <h2><%= list.name %></h2>

                <% list.items.forEach(function(item){ %>
                    <form action="/delete" method="post">
                        <div class="item">
                            <input type="checkbox" name="checkbox" value="<%=item._id%>" onChange="this.form.submit()">
                            <p><%=item.name%></p>
                            <input type="hidden" name="listName" value="<%= list.name %>">
                        </div>
                    </form>
                <% }) %>

                <form class="form-add" action="/" method="post">
                    <input type="text" name="newItem" placeholder="Nueva tarea..." autocomplete="off" required>
                    <button type="submit" name="list" value="<%= list.name %>">+</button>
                </form>
            </div>

        <% }) %>

    </div>

    <div class="new-list-section">
        <p style="color:white; margin-bottom: 20px;">¿Necesitas otra lista? Escribe en la URL: /loQueQuieras</p>
    </div>

</body>
</html>